import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header className="header">
            <h1>Nombre del Aplicativo</h1>
        </header>
    );
};

export default Header;
